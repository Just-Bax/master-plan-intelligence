/**
 * Central type exports. Prefer importing from "@/types" for reuse.
 *
 * - api: API entities and request/response shapes (User, MasterPlan, Project, etc.)
 * - map: Map/layer types (GeometryEditTarget, NewMasterPlanPlacement, MapFlyOptions, etc.)
 * - chat: AI chat types (ChatMessage, SelectedObjectContext)
 * - i18n: Locale type
 */

export * from "@/types/api";
export * from "@/types/map";
export * from "@/types/chat";
export * from "@/types/i18n";
